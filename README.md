# .github

Shared CI / default community files for **emmett-lathrop-brown** repos.

- reusable workflows: commit-lint / release / taplo / update-tap
- `roadmap-add` — 全 public repo の open issue を Projects #1 (roadmap) へ自動 sweep
- `actions/swift-build` — composite build action
